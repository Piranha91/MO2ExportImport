<UserControl x:Class="MO2ExportImport.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:main="clr-namespace:MO2ExportImport"
             xmlns:vms = "clr-namespace:MO2ExportImport.ViewModels"
             d:DataContext="{d:DesignInstance Type=vms:ImportViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <main:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter" />
        <main:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <!-- Define a custom style for ListBoxItem -->
        <Style TargetType="ListBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Name="Border" Padding="2" SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Trigger for when the item is selected -->
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="LightBlue"/>
                                <!--<Setter TargetName="Border" Property="Foreground" Value="White"/>-->
                            </Trigger>
                            <!-- Trigger for when the item is selected but the ListBox is out of focus -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsFocused" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="LightBlue"/>
                                <!--<Setter TargetName="Border" Property="Foreground" Value="White"/>-->
                            </MultiTrigger>
                            <!-- Trigger for mouse over -->
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="LightGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- MO2 Directory Selection -->
            <RowDefinition Height="Auto"/>
            <!-- Profile Selection -->
            <RowDefinition Height="Auto"/>
            <!-- Import Source Folder Button -->
            <RowDefinition Height="Auto"/>
            <!-- Import Options boxes -->
            <RowDefinition Height="Auto"/>
            <!-- Import Mode Button -->
            <RowDefinition Height="Auto"/>
            <!-- Checkbox Explanation Text -->
            <RowDefinition Height="*"/>
            <!-- Mods ListBox -->
            <RowDefinition Height="Auto"/>
            <!-- Import Button -->
        </Grid.RowDefinitions>

        <!-- MO2 Directory Selection -->
        <StackPanel Orientation="Horizontal" Margin="5">
            <Button Content="Select MO2 Directory" Command="{Binding SelectMo2DirectoryCommand}" Margin="5"/>
            <TextBlock Text="{Binding Mo2Directory}" VerticalAlignment="Center" Margin="5,0"/>
        </StackPanel>

        <!-- Profile Selection -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
            <TextBlock Text="Profile:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" Margin="5"/>
        </StackPanel>

        <!-- Import Source Folder Selection -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="5">
            <Button Content="Select Import Source Folder" Command="{Binding SelectImportSourceFolderCommand}" Margin="5"/>
            <TextBlock Text="{Binding ImportSourceFolder}" VerticalAlignment="Center" Margin="5,0"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0" Visibility="{Binding IsSourceMo2Directory, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="Source Profile:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox ItemsSource="{Binding SourceProfiles}" SelectedItem="{Binding SelectedSourceProfile}" MinWidth="150" Margin="5"/>
            </StackPanel>
        </StackPanel>

        <WrapPanel Orientation="Horizontal" Grid.Row="3" Margin="5">
            <CheckBox Content="Ignore Disabled" IsChecked="{Binding IgnoreDisabled}" Margin="10 0 0 0"/>
            <CheckBox Content="Ignore Separators" IsChecked="{Binding IgnoreSeparators}" Margin="10 0 0 0"/>
            <CheckBox Content="Add [NoDelete] Flags" IsChecked="{Binding AddNoDeleteFlags}" Margin="10 0 0 0"/>
            <CheckBox Content="Remove [NoDelete] Flags" IsChecked="{Binding StripNoDelete}" Margin="10 0 0 0"/>
            <CheckBox Content="Disallow All Mod Overwrites" IsChecked="{Binding SkipExisting}" Margin="10 0 0 0"/>
            <CheckBox Content="Ignore Splice Order of Existing Mods" IsChecked="{Binding IgnoreMatchedModsForOrdering}" Margin="10 0 0 0"/>
            <CheckBox Content="Match [Enabled/Disabled] Mods From Import" IsChecked="{Binding MatchModActivationState}" Margin="10 0 0 0"/>
            <CheckBox Content="Match [Enabled/Disabled] Plugins From Import" IsChecked="{Binding MatchPluginActivationState}" Margin="10 0 0 0"/>
            <CheckBox Content="Interpolate Missing Plugin Groups" IsChecked="{Binding InterpolateMissingPluginGroups}" Margin="10 0 0 0"/>
            <CheckBox Content="Calculate Available Space Automatically" IsChecked="{Binding AutoCalculateSpace}" Margin="10 0 0 0"/>
            <TextBlock Text="Add Prefix to Imported Mod Names:" Margin="10 0 0 0"/>
            <TextBox Text="{Binding ImportPrefix}" MinWidth="100" Margin="5 0 0 0"/>
        </WrapPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="5">
            <TextBlock Text="Import Mode:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox ItemsSource="{Binding ImportModes}" SelectedItem="{Binding SelectedImportMode}" MinWidth="70" Margin="5"/>
            <Grid Margin="5 0 0 0">
                <TextBox x:Name="FilterTextBox"
                 Width="200"
                 Margin="5"
                 VerticalContentAlignment="Center"
                 Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                 TextChanged="FilterTextBox_TextChanged"/>

                            <TextBlock Text="Filter mods..."
                   IsHitTestVisible="False"
                   Foreground="Gray"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   Margin="10,0,0,0"
                   Visibility="{Binding Text, ElementName=FilterTextBox, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}" />
            </Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,5,0">
                <TextBlock Text="Multi Plugin Mode:" VerticalAlignment="Center" Margin="5,0"/>
                <ComboBox ItemsSource="{Binding MultiPluginModes}" SelectedItem="{Binding SelectedMultiPluginMode}" MinWidth="100" Margin="5,0"/>
        
                <TextBlock Text="Plugin Selection:" VerticalAlignment="Center" Margin="15,0,5,0"/>
                <ComboBox ItemsSource="{Binding PluginSelectionModes}" SelectedItem="{Binding SelectedPluginSelectionMode}" MinWidth="100" Margin="5,0"/>
        
                <Button Content="Add Master Dependencies" Command="{Binding AddMasterDependenciesCommand}" Margin="15,0,5,0" Padding="10,5"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Checkbox Explanation -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="10">
            <CheckBox IsChecked="False" IsEnabled="False" Content="Mod is Activated" Margin="0 0 10 0" VerticalAlignment="Center"/>
            <CheckBox IsChecked="False" IsEnabled="True" Content="Mod will Overwrite Existing (if present)" VerticalAlignment="Center"/>
            <Button Content="Set as Overwriting Mods" Command="{Binding SetSelectedAsOverrideCommand}" Margin="20 0 10 0" />
            <Button Content="Unset as Overwriting Mods" Command="{Binding UnsetSelectedAsOverrideCommand}" Margin="20 0 0 0" />
        </StackPanel>

        <!-- Mod List -->
        <ListBox Grid.Row="6" ItemsSource="{Binding FilteredModList}" Margin="10"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 SelectionMode="Extended" 
                 Name="ModsListBox"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 SelectionChanged="ModsListBox_SelectionChanged">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Name="Border" Padding="2" SnapsToDevicePixels="true">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="true">
                                        <Setter TargetName="Border" Property="Background" Value="LightBlue"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="true"/>
                                            <Condition Property="IsFocused" Value="false"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="Border" Property="Background" Value="LightBlue"/>
                                    </MultiTrigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter TargetName="Border" Property="Background" Value="LightGray"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding EnabledInExportedModList}" IsEnabled="False" Margin="0,0,10,0"/>
                        <CheckBox IsChecked="{Binding OverWriteExistingDuringImport}"  Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- "Please Wait" TextBlock -->
        <TextBlock x:Name="PleaseWaitText"
           Grid.Row="6"
       Text="Please Wait"
       HorizontalAlignment="Center"
       VerticalAlignment="Center"
       Foreground="Red"
       FontSize="20"
       Visibility="{Binding IsPleaseWaitVisible, Converter={StaticResource BoolToVisibilityConverter}, Mode=TwoWay}"
       IsHitTestVisible="False" />
        
        <StackPanel x:Name="ProgressDetailsPanel"
                    Grid.Row="6"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0,40,0,0"
                    Visibility="{Binding ShowProgressDetails, Converter={StaticResource BoolToVisibilityConverter}}"
                    IsHitTestVisible="False">
            <TextBlock Text="{Binding ProgressStatusText}"
                       Foreground="DarkRed"
                       FontSize="14"
                       TextAlignment="Center"
                       Margin="0,0,0,5"/>
            <TextBlock Text="{Binding ProgressDetailText}"
                       Foreground="DarkRed"
                       FontSize="11"
                       TextAlignment="Center"/>
        </StackPanel>

        <!-- Canvas to draw the blue lines -->
        <Canvas Grid.Row="6" IsHitTestVisible="False" Name="HighlightCanvas" Width="{Binding ActualWidth, ElementName=ModsListBox}" 
Height="{Binding ActualHeight, ElementName=ModsListBox}" Opacity="1">
            <ItemsControl ItemsSource="{Binding ElementName=ModsListBox, Path=SelectedItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Rectangle Width="4" Fill="Blue" Height="2" Canvas.Left="0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>

        <!-- Import Button -->
        <Button Grid.Row="7" Content="{Binding ImportButtonLabel}" Command="{Binding LaunchImportPopupCommand}" HorizontalAlignment="Center" Margin="5" IsEnabled="{Binding IsImportEnabled}"/>
        
        <!-- Filtering Notification -->
        <TextBlock Grid.Row="7" Text="Checking for Duplicate Mods. Please Wait." Foreground="Red" HorizontalAlignment="Right" Visibility="{Binding ShowFilteringNotification}" IsHitTestVisible="False"/>
    </Grid>
</UserControl>