<UserControl x:Class="MO2ExportImport.Views.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:reactiveui="http://reactiveui.net"
             xmlns:main="clr-namespace:MO2ExportImport"
             xmlns:vms = "clr-namespace:MO2ExportImport.ViewModels"
             d:DataContext="{d:DesignInstance Type=vms:ExportViewModel}"
             mc:Ignorable="d">

    <UserControl.Resources>
        <main:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter" />
        <main:TopOffsetConverter x:Key="TopOffsetConverter"/>
        <main:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <!-- Define a custom style for ListBoxItem -->
        <Style TargetType="ListBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Name="Border" Padding="2" SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Trigger for when the item is selected -->
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="LightBlue"/>
                                <!--<Setter TargetName="Border" Property="Foreground" Value="White"/>-->
                            </Trigger>
                            <!-- Trigger for when the item is selected but the ListBox is out of focus -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsFocused" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="LightBlue"/>
                                <!--<Setter TargetName="Border" Property="Foreground" Value="White"/>-->
                            </MultiTrigger>
                            <!-- Trigger for mouse over -->
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="LightGreen"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Define row definitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- For MO2 Selection -->
            <RowDefinition Height="Auto"/> <!-- For Profile Selection -->
            <RowDefinition Height="Auto"/> <!-- For Checkbox Options -->
            <RowDefinition Height="Auto"/> <!-- For Action Buttons -->
            <RowDefinition Height="Auto"/> <!-- For filter -->
            <RowDefinition Height="Auto"/> <!-- For checkbox explanations -->
            <RowDefinition Height="*"/>  <!-- For the ListBox to take remaining space -->          
            <RowDefinition Height="Auto"/> <!-- For the Export button -->
            <RowDefinition Height="Auto"/> <!-- For the Export destination-->
        </Grid.RowDefinitions>

        <!-- Top section with button and ComboBox -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Select MO2 Directory" 
                    Command="{Binding SelectSourceCommand}"
                    ToolTip="Select the root directory of your Mod Organizer 2 installation"/>
            <TextBlock Text="{Binding Mo2Directory}" 
                       VerticalAlignment="Center" 
                       Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Profile:" Margin="10" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Profiles}" 
                      SelectedItem="{Binding SelectedProfile}" 
                      Margin="10"
                      ToolTip="Select the MO2 profile to export mods from"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10">
            <CheckBox Content="Ignore Disabled" 
                      IsChecked="{Binding IgnoreDisabled}"
                      ToolTip="When checked, disabled mods will not be exported"/>
            <CheckBox Content="Ignore Separators" 
                      IsChecked="{Binding IgnoreSeparators}" 
                      Margin="10 0 0 0"
                      ToolTip="When checked, separator entries will not be exported"/>
            <CheckBox Content="Calculate Available Space Automatically" 
                      IsChecked="{Binding AutoCalculateSpace}" 
                      Margin="10 0 0 0"
                      ToolTip="Automatically calculate and display available disk space before exporting"/>
        </StackPanel>

        <!-- ListBox with constrained height in the remaining space -->
        <Grid Grid.Row="3">
            <TextBox x:Name="FilterTextBox"
                     Width="200"
                     Margin="5"
                     VerticalContentAlignment="Center"
                     HorizontalAlignment="Left"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     TextChanged="FilterTextBox_TextChanged"
                     ToolTip="Filter the mod list by typing a search term"/>

            <TextBlock Text="Filter mods..."
                       IsHitTestVisible="False"
                       Foreground="Gray"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="10,0,0,0"
                       Visibility="{Binding Text, ElementName=FilterTextBox, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}" />
        </Grid>

        <StackPanel Grid.Row="4" VerticalAlignment="Top" Orientation="Horizontal" Margin="10">
            <Button Content="Undo" 
                    Command="{Binding UndoSelectionCommand}" 
                    Margin="0 0 5 0" 
                    ToolTip="Undo selection change (Ctrl+Z)"/>
            <Button Content="Redo" 
                    Command="{Binding RedoSelectionCommand}" 
                    Margin="0 0 10 0" 
                    ToolTip="Redo selection change (Ctrl+Y)"/>
            <Button Content="Set as Overwriting Mods" 
                    Command="{Binding SetSelectedAsOverrideCommand}" 
                    Margin="0 0 10 0"
                    ToolTip="Mark selected mods to overwrite existing files during import"/>
            <Button Content="Unset as Overwriting Mods" 
                    Command="{Binding UnsetSelectedAsOverrideCommand}" 
                    Margin="0 0 10 0"
                    ToolTip="Unmark selected mods from overwriting existing files during import"/>
            <Button Content="Toggle Parent Separators" 
                    Command="{Binding ToggleParentSeparatorsCommand}" 
                    Margin="0 0 10 0"
                    ToolTip="Toggle selection of parent separators for selected mods"/>
            <Button Content="{Binding ExportButtonLabel}" 
                    Command="{Binding ExportSelectedCommand}" 
                    Margin="50 0 0 0"
                    ToolTip="Export the selected mods to the destination folder"/>
        </StackPanel>

        <!-- Checkbox Explanation -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="10 0 0 0">
            <CheckBox IsChecked="False" IsEnabled="False" Content="Mod is Activated" Margin="0 0 10 0"/>
            <CheckBox IsChecked="False" IsEnabled="True" Content="Mod will Overwrite Existing (if present)"/>
        </StackPanel>
        
        <ListBox Grid.Row="6" 
                 ItemsSource="{Binding FilteredModList}" 
                 Margin="10"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 SelectionMode="Extended" 
                 Name="ModsListBox"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 SelectionChanged="ModsListBox_SelectionChanged"
                 ToolTip="Select mods to export (Ctrl+Click for multiple, Shift+Click for range, right-click separators for group selection)">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <!-- Remove the IsSelected binding -->
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Name="Border" Padding="2" SnapsToDevicePixels="true">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="true">
                                        <Setter TargetName="Border" Property="Background" Value="LightBlue"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="true"/>
                                            <Condition Property="IsFocused" Value="false"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="Border" Property="Background" Value="LightBlue"/>
                                    </MultiTrigger>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter TargetName="Border" Property="Background" Value="LightGray"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding EnabledInExportedModList}" IsEnabled="False" Margin="0,0,10,0"/>
                        <CheckBox IsChecked="{Binding OverWriteExistingDuringImport}"  Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- "Please Wait" TextBlock -->
        <TextBlock x:Name="PleaseWaitText"
                   Grid.Row="6"
                   Text="Please Wait"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Red"
                   FontSize="20"
                   Visibility="{Binding IsPleaseWaitVisible, Converter={StaticResource BoolToVisibilityConverter}, Mode=TwoWay}"
                   IsHitTestVisible="False" />

        <!-- Canvas to draw the blue lines -->
        <Canvas Grid.Row="6" IsHitTestVisible="False" Name="HighlightCanvas" Width="{Binding ActualWidth, ElementName=ModsListBox}" 
                Height="{Binding ActualHeight, ElementName=ModsListBox}" Opacity="1">
            <ItemsControl ItemsSource="{Binding ElementName=ModsListBox, Path=SelectedItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Rectangle Width="4" Fill="Blue" Height="2" Canvas.Left="0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>

        <!-- Export Selected Button -->

        <!-- Bottom panel for Export Destination Folder selection -->
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="7" Orientation="Horizontal"  VerticalAlignment="Center" Margin="5 0 0 5">
            <TextBlock Text="Export Destination:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding ExportDestinationFolder, Mode=TwoWay}" 
                     Width="400" 
                     Margin="10,0" 
                     VerticalContentAlignment="Center"
                     ToolTip="The folder where exported mods will be saved"/>
            <Button Content="Browse" 
                    Command="{Binding BrowseFolderCommand}"
                    ToolTip="Browse for a folder to save exported mods"/>
        </StackPanel>
    </Grid>
</UserControl>